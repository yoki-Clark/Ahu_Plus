# Ahu_Plus 界面美化 · 四批次执行手册

> 本文件是界面美化落地的工程执行手册,按 4 个批次组织,供逐批次完成。
> 来源:方向清单(已审阅)→ 用户选定 41 项 → 细化落点。
> 行号引用为盘点时快照,**以当前代码为准**(代码会随实现变动)。

---

## 0. 全局硬约束(每个批次都必须遵守)

1. **Material Expressive API 不可用** — BOM 2026.02.01 下 `MaterialExpressiveTheme`/`MotionScheme`/`LoadingIndicator`/`Shapes.largeIncreased` 均为 internal。所有 Expressive 动效/圆角用**手写 spring/tween** 或 `AhuShapes` 语义 token 替代。
2. **Tab 内容切换不用 Crossfade** — 会触发 ViewModel 重复 activate。Tab 区动效**只动指示器**,不碰内容淡入。
3. **渐变不作所有卡片默认背景** — 可读性风险。渐变限于:Hero 卡、按钮、描边、页面背景光斑。
4. **`Modifier.blur`/`RenderEffect` 仅 API 31+** — minSdk 24,玻璃/模糊效果低版本(<31)**降级为半透实色**。
5. **无 Lottie 依赖** — 复杂动画一律手写(Compose 动画 API 全自带,零成本)。
6. **深色判定**用 `MaterialTheme.colorScheme.background.luminance() < 0.5f`(`StatusColors.kt:16-18`),非 `isSystemInDarkTheme()`,以兼容显式主题覆盖。
7. **状态色双档**走 `AhuToneColor`(`StatusColors.kt:20-31`),不要新造硬编码 `Color(0xFF...)`。

---

## 1. 全局决策点(已采默认,实现时可调整)

| # | 决策点 | 默认建议 |
|---|---|---|
| D1 | 大圆角幅度(批次一·1) | 列表 12→14、卡片 16→18、Hero 保持 24。保守档。 |
| D2 | 多主题色备选(批次一·8) | 蓝(默认)/青/紫/橙/粉 5 色,前 4 个品牌色已备齐。 |
| D3 | 插画风格(批次二·43) | 扁平双色矢量场景(非吉祥物),与品牌渐变色系一致。 |
| D4 | 焦点轮播是否自动(批次三·19) | 仅手动滑,不做自动轮播(避免分散注意)。 |
| D5 | 动态天气图标首批(批次四·44) | 先做晴/多云/雨/雪 4 种高频,其余后续补。 |
| D6 | 彩蛋触发场景(批次四·10) | 开学季 + 考试周 + 校庆(9 月)3 个,不做全节日。 |
| D7 | 闪屏过渡(批次四·34) | 做,但严控不拖慢冷启动(<500ms 过渡)。 |

---

## 2. 批次总览

| 批次 | 主题 | 含工作流 | 方案数 | 性质 |
|---|---|---|---|---|
| 一 | 设计系统还债与扩展 | W1 | 12 | 打底,本身不大改观感,为后续铺路 + 修深色隐患 |
| 二 | 质感微交互 + 状态统一 | W2 + W3 | 16 | 观感提升最大,出效果 |
| 三 | 沉浸式与布局节奏 | W4 | 8 | 沉浸感 + 信息节奏 |
| 四 | 动效转场与特色 + 彩蛋 | W5 + W6 | 5 | 特色动效 + 节日氛围 |

建议严格按 一→二→三→四 顺序:批次一是底座,后续每批都依赖它的 token/组件。

---

## 3. 批次一 · 设计系统还债与扩展(W1)

> 性质:还债 + 扩 token。完成后深色隐患消除、一致性达标、后续批次有统一组件可用。

### 71. 深色模式硬编码色修复
- **效果**:深色下这批色不再突兀发暗或对比度不足,融入色板;浅色不变。
- **落点**:`ProfileHomeScreen.kt:421/427`(第三方紫 `0xFF9B59B6`)、`:567`(关于灰 `0xFF6078B`)、`WeatherScreen.kt:243-244`(AQ 橙/绿)、`:312`(降雨蓝)、`DashboardScreen.kt:518`(倒计时黄) → 全改走 `AhuToneColor` 双档。
- **成本:低** | 纯还债,是潜在 bug 修复。**应作为本批第一项。**

### 74. 间距/Shape token 收口
- **效果**:全 App 卡间距统一 12dp 节奏,圆角不再有字面量与 token 混用。
- **落点**:卡间距 Dashboard 12 / Profile 16 / JwcNotice 10 / Market 14 / Weather 12 → 统一 `AhuSpacing.CardGap`;`WeatherScreen.kt:126/205/262/349` 字面量 `RoundedCornerShape(16.dp)` → `AhuShapes.LargeCard`。
- **成本:低** | 与 1 协同。

### 1. 大圆角"有机"亲和风
- **效果**:边角更圆润,气质从"冷工具感"转"柔和亲和"。
- **落点**:`Shape.kt` token 上调(`medium 12→14`、`large 16→18`),`AhuShapes.Card 12→14`、`LargeCard 16→18`;HeroCard 24 保持(最高档)。
- **依赖**:先做 74。**成本:低** | 幅度见 D1。

### 14. 多层投影精细化
- **效果**:卡片有真实纵深 — 环境光漫投影 + 方向投影,像轻浮桌面。层次梯度从"1dp/4dp 两档"变"漫射/中/高"三档立体。
- **落点**:新建 `AhuElevation` token(ambient + directional 双值);`AhuUi.kt:107`(AhuCard)、`:339`(AhuHeroCard) 改用双 `Modifier.shadow` 叠加或 `drawBehind` 自绘双阴影。
- **成本:中低** | Compose shadow 不原生支持双阴影,需叠加;深色下阴影弱,要验证。

### 16. 渐变/光晕描边
- **效果**:强调卡片边框是品牌渐变描边而非纯色线,像"发光勾勒",焦点突出。用于成绩选中柱、应用中心选中磁贴、课表当前节。
- **落点**:新增 `Modifier.gradientBorder(brush, width)` 扩展(`drawBehind` 画描边);接入选中态卡片。
- **成本:低** | 与 13 共享 Gradient 体系。

### 13. 主色渐变化
- **效果**:主要按钮/强调操作(刷新、签到、提交)从纯蓝变 Blue 渐变,与 Hero 渐变呼应,品牌感统一有活力。次要按钮仍纯色以克制。
- **落点**:扩展 `AhuGradient` 提供按钮用渐变;关键 Button 处用 `Modifier.background(brush)` 或自定义 ButtonColors。
- **成本:低** | ⚠️ 渐变按钮上文字对比度需验证。

### 8. 多主题色用户可切换 ⭐
- **效果**:设置新增"主题色"(蓝/青/紫/橙/粉),选后全局 primary/container/按钮/Hero 渐变随之换色。默认仍安大蓝。
- **落点**:复用 `AppThemeMode` 链路 — 新增 `AppAccentColor` 枚举存 `AppDataStore`、`SessionManager` 暴露 `accentColorFlow`、`AhuPlusTheme`(`Theme.kt:37-58`)按 accent 重组 colorScheme 的 primary/secondary/container;`AppSettingsScreen.kt:65` 旁加选择器。品牌色已备齐(`Color.kt`)。
- **成本:低中** | 基础设施现成;⚠️ 需确保所有硬编码 primary 处走 colorScheme(与 71 协同)。

### 73. 统一 ListRow 组件
- **效果**:成绩行/通知行/集市卡/设置行这些"图标+主副标题+尾部"卡片行长得一样 — 同样的左图标盒、间距、尾部对齐,消除"差不多又不完全一样"的违和,减重复代码。
- **落点**:`AhuUi.kt` 新增 `AhuListRow`(图标盒 + 双行文本 + 尾部槽 + 可选 accent 条);逐步替换 `GradeRow`(`GradeScreen.kt:644`)、`NoticeRow`(`JwcNoticeListScreen.kt:267`)、`MarketTopicCard`、`SettingsRow`。
- **成本:中** | 渐进替换,不一次全改。是批次二 17/22 的载体。

### 41. 图标线性/填充风格统一
- **效果**:全 App 图标不再 outlined/filled 混用,定一套规则,视觉更干净。
- **落点**:规则 — 导航/未选中=outlined,选中/强调=filled(底部导航已是此规则 `MainNavigationComponents.kt:86-118`,推广到设置行、列表行)。
- **成本:低中** | 梳理 + 机械替换。

### 42. 入口图标渐变底全面统一
- **效果**:所有功能入口图标(应用中心、首页收藏坞、设置快捷入口)同一套渐变底规则,消除"应用中心即时 lerp、别处不一样"的差异。
- **落点**:`AppHubScreen.kt:996-1001` 即时 `lerp` 渐变 → 抽 `AhuGradient.forTint(color)` 统一 token,所有入口复用。
- **成本:低**。

### 39. 等宽数字全面推广
- **效果**:所有金额/GPA/成绩/时间不再因 1/0 宽度不同抖动,对齐整齐。
- **落点**:`tabularFigures()`(`Type.kt:102`)已有,推广到 HomeScreen 余额/账单、GradeScreen 分数、课表时间、一卡通账单等所有数字密集处。
- **成本:低** | 机械推广。

### 40. 字号无障碍可调
- **效果**:设置加"字体大小"滑块(或接系统字号),全局文字随需放大,无障碍达标。
- **落点**:Compose 原生 `fontScale`,设置暴露滑块并写回;验证各卡片大字号下不溢出(配合 maxLines/自适应)。
- **成本:低中** | 开关易,大字号布局适配是工作量。

**批次一完成标志**:深色无突兀色、卡间距/圆角全 token 化、`AhuListRow`/`AhuElevation`/`gradientBorder`/`AhuAccentColor` 可用、主色可切换。

---

## 4. 批次二 · 质感微交互 + 状态统一(W2 + W3)

> 性质:观感提升最大的一批。质感(投影/按压/留白)+ 动效(进场/骨架/平滑)+ 状态(插画/统一)。

### 17. 卡片按压反馈
- **效果**:点卡片时轻微缩 0.98 + 阴影收缩,松手 spring 回弹,像按软按键,操作有回应感。全局统一手感。
- **落点**:`AhuCard`/`AhuListRow` 内置 `Modifier.pressableScale()`(基于 `interactionSource` + `animateFloatAsState` spring);全局卡片自动获得。
- **依赖**:73 提供载体。**成本:低** | 质感提升最明显之一。

### 26. 弹性 spring 反馈
- **效果**:按钮按压、卡片回弹、开关切换用 spring 物理曲线(轻微过冲)而非匀速,动效"活"起来,手写替代不可用的 MotionScheme。
- **落点**:统一 `AhuMotion` token(几档 `spring(dampingRatio, stiffness)`),替换关键处 `tween`。
- **成本:低** | 与 17/22/30 共用。

### 20 + 50. 留白呼吸感 / 大留白呼吸版式
- **效果**:卡片内 padding 加大、卡间靠间距而非边框分层、关键页(个人中心/关于/引导)降信息密度,从"挤"变"透"。
- **落点**:推广 `AhuSpacing.Card=16dp` 内边距;移除多余 `HorizontalDivider` 改间距分层;个人中心/关于/引导页增留白。
- **成本:低** | 与 74 协同。

### 21. 分类色条/顶部色带
- **效果**:通知按分类左侧色条,消息按类型色标,扫一眼知类别。AgendaCard 已有(`AgendaCard.kt:116-121`),推广开。
- **落点**:推广 AgendaCard 的 4dp accent 条到 `JwcNoticeListScreen`、`UnifiedMessageCenterScreen`;`AhuListRow` 加可选 accent 槽。
- **成本:低** | 模式已存在。

### 22. 列表项交错进场
- **效果**:打开成绩页时 GPA 卡→柱状图→chips→各 GradeRow 依次从下滑入淡入,像内容"涌上来",而非一次砸出。通知/集市同效。
- **落点**:`LazyColumn` 的 `animateItem`(原生) + 首屏 `LaunchedEffect` 触发交错延迟;落 GradeScreen/JwcNoticeListScreen/MarketListScreen。
- **成本:低** | ⚠️ 长列表只对首屏可见项交错,避免延迟累积。

### 24. 骨架屏 Skeleton
- **效果**:加载时显示灰色占位"骨架"(卡片轮廓+灰条)而非孤零零转圈,体感"内容在成形",比转圈快且稳。
- **落点**:新增 `AhuSkeleton`/`AhuSkeletonCard`(灰条 + shimmer 微光扫动);替换 GradeScreen/WeatherScreen/JwcNotice 的 `CenteredLoader`、HomeScreen 余额加载。
- **依赖**:与 72 协同(骨架作 loading 态统一组件)。**成本:低中** | shimmer 用 `Brush.linearGradient` + `rememberInfiniteTransition`。

### 35. 状态切换平滑过渡
- **效果**:加载→内容→错误 间不再硬切闪一下,而是 `AnimatedContent` 平滑交叉淡入。**仅限非 Tab 内容区**(Tab 区仍硬切)。
- **落点**:页面级 `AnimatedContent(uiState)` 包裹;落 GradeScreen/WeatherScreen/JwcNotice 等单页状态机。
- **成本:低** | ⚠️ 严格避开主 Tab 内容区。

### 28. 下拉刷新自定义指示器
- **效果**:下拉刷新圈从系统默认换品牌渐变加载圈,刷新有品牌识别感。
- **落点**:`AhuPullToRefreshBox.kt:36` 的 `PullToRefreshDefaults.Indicator` → 自定义 Indicator(品牌渐变弧 + 旋转)。
- **成本:低**。

### 53. 后台刷新顶部细进度条
- **效果**:有缓存后台刷新时,顶部 2dp 品牌色细进度条滑过(类 YouTube/Gmail),而非居中弹转圈遮挡内容,刷新不打断阅读。
- **落点**:新增 `AhuLinearProgressIndicator`(顶部 indeterminate 细条);接各页 `isRefreshing && hasCache` 分支。
- **成本:低**。

### 30. 进度环/倒计时美化
- **效果**:`CountdownArc` 推广更多进度场景;今日课程进度条/倒计时从 30s 跳变升级为**平滑动画驱动**(进度丝滑推进、倒计时连续递减)。
- **落点**:已有 `CountdownArc`;`TodayCourseCard.kt:151` 进度条 + `:170-175` 倒计时 → 改 `animateFloatAsState` 平滑驱动(基于 `DebugClock` 实时算比例,非 30s tick 重算)。
- **成本:低中** | 今日课程卡是焦点,平滑化收益高。

### 72. 状态呈现四套统一 ⭐
- **效果**:加载/空/错误态全 App 一套长相 — 同图标盒、同插画、同文案节奏、同重试按钮。不再"成绩页有插画、通知页只有字+小转圈、余额页连图标都没有"。
- **落点**:统一到 `AhuUi.kt` 的 `AhuEmptyState`/`AhuErrorState`/`CenteredLoader`;替换 `JwcNoticeListScreen.kt:524-570`(LoadingRow/ErrorBlock)、Market 局部 StatusCard、`HomeScreen.kt:208-217`(余额纯内联 Text+重试)、`DashboardScreen.kt:351-359`(内联小转圈)。
- **依赖**:43/52 提供插画,24 提供骨架。**成本:中** | 高价值,结构性减代码 + 一致性。

### 43. 空状态插画(不要吉祥物)+ 52. 错误状态插画 + 45. SVG 矢量插画
- **效果**:空数据(无课/无通知/无成绩)和错误(网络失败/未登录/权限拒绝)显示**矢量场景插画**(非吉祥物 — 如空通知画空收件箱+飘纸、网络错误画断线插头),配文案+操作按钮,告别干巴巴纯文字。关于页/引导页用更精致 SVG(Coil 已支持)。
- **落点**:绘制一组矢量插画(`ImageVector` 或 SVG 资源 `res/drawable`);接入 `AhuEmptyState`/`AhuErrorState`;引导/关于页用 Coil 加载 SVG。
- **依赖**:72 提供容器。**成本:中** | 插画绘制是主要工作量,可分批(先空状态后错误态)。风格见 D3。

### 55. 全屏错误覆盖核查
- **效果**:确保"有缓存时刷新失败不全屏覆盖内容",保留缓存+底部 `DataStatusFooter` 提示,用户不被打断。一致性核查项。
- **落点**:核查各数据页错误分支;`docs/13` 已要求。
- **成本:低** | 核查 + 修正违规处。

**批次二完成标志**:卡片有按压/投影质感、列表有进场动效、加载有骨架、空错有插画、状态全统一、今日课程进度平滑、下拉刷新/后台刷新有品牌识别。

---

## 5. 批次三 · 沉浸式与布局节奏(W4)

> 性质:沉浸感 + 信息节奏。依赖批次一的 token 与批次二的组件。

### 56. 状态栏/导航栏沉浸式(edge-to-edge)
- **效果**:内容延伸到状态栏/导航栏后方,状态栏透明、背景色透出,沉浸感强,无顶部突兀色块。
- **落点**:`MainActivity` 启用 `enableEdgeToEdge()`;各屏用 `WindowInsets` 处理状态栏/导航栏 padding。
- **成本:低中** | ⚠️ 逐屏验证内容不被遮挡。**本批第一项,后续都依赖它。**

### 57. 顶部栏滚动渐变
- **效果**:静止时顶栏透明(内容透出),滚动时渐变为实色+毛玻璃+细边,像 iOS 收起效果,层次分明。
- **落点**:`AhuTopAppBar`(`AhuUi.kt`)加 `scrollBehavior` + 透明度动画;低版本(<31)毛玻璃降级半透实色。
- **依赖**:56。**成本:中** | 接 `TopAppBarScrollBehavior`。

### 58. Hero 区顶满到状态栏
- **效果**:个人中心/首页渐变 Hero banner 延伸到状态栏后方,头像/标题"浸"在渐变里,沉浸感拉满,而非 banner 下还留一条状态栏背景。
- **落点**:`ProfileHomeScreen.kt:690`(ProfileHeader Blue 渐变)、`DashboardScreen` 今日课程 Hero → 顶部 padding 用 `statusBarsPadding` 而非固定 top padding,渐变顶满。
- **依赖**:56。**成本:低中** | 处理状态栏字色(渐变上用浅色字)。

### 60. 手势导航 + 边缘到边缘适配
- **效果**:全面屏手势导航下内容不被手势条遮挡,返回手势流畅;与 56 协同的收尾项。
- **落点**:验证 `navigationBarsPadding`/`displayCutoutPadding` 各屏;`MainScreen` 返回行为。
- **成本:低** | 适配核查。

### 46. 横向滑动卡片轨
- **效果**:首页增加"今日推荐"式横滑卡片轨(快捷功能/最近通知/收藏应用),左右滑浏览,类 App Store 今日推荐,首页信息密度更丰富有层次。
- **落点**:`DashboardScreen` 新增 `LazyRow` 卡片轨段(首页已有 FavoritesDock 收藏网格,横滑轨是补充不重复)。
- **成本:低中** | ⚠️ 与 19 协调首页布局,避免顶部太挤。

### 48. 分组吸顶标题
- **效果**:通知列表/设置页滚动时当前分组标题"吸"在顶部,直到下一组顶上来,长列表里始终知自己在哪个分类。
- **落点**:`LazyColumn` 的 `stickyHeader`;落 `JwcNoticeListScreen`(按分类)、`AppSettingsScreen`(按设置组)。
- **成本:低** | `stickyHeader` 实验性,需 `@OptIn`。

### 19. 焦点卡横向轮播
- **效果**:首页顶部 Hero 区从单张"今日课程"变可横滑焦点轮播(今日课程/余额/天气/日程概览),带底部小圆点指示器,首页第一屏信息量提升。
- **落点**:`DashboardScreen` 今日课程区 → `HorizontalPager` + 指示点;`TodayCourseCard` 本身复杂,作为 pager 一页。
- **依赖**:与 46 协调首页布局。**成本:中** | ⚠️ 自动轮播见 D4(默认不做,仅手动滑);`HorizontalPager` 原生。

### 18. 卡片展开/折叠(accordion)
- **效果**:长内容默认折叠显摘要,点击展开详情 — 通知正文/成绩明细/课表课程详情。减首屏过载,需要时再展开。
- **落点**:JwcNotice 详情(`DashboardScreen.kt:445` 已用 AnimatedVisibility)、成绩明细(现走 ModalBottomSheet,可加折叠)、课表课程长按详情。
- **成本:低** | AnimatedVisibility 模式已用熟。

**批次三完成标志**:edge-to-edge 全启用、顶栏滚动渐变、Hero 顶满状态栏、首页有横滑轨+焦点轮播、长列表有吸顶分组、长内容可折叠。

---

## 6. 批次四 · 动效转场与特色 + 彩蛋(W5 + W6)

> 性质:特色动效 + 节日氛围。锦上添花,依赖前三批基础。

### 27. Tab 指示器滑动
- **效果**:子页 Tab(超星/课表设置/开发者中心)切换时选中下划线平滑滑动到新位置,而非硬跳。**仅动指示器,内容仍硬切**。
- **落点**:`ChaoxingTabScreen`/`ScheduleSettingsSheet`/`DeveloperCenterScreen` 的 `TabRow` → 自定义 `indicator` 用 `Modifier.offset` + `animateDpAsState` 滑动。
- **成本:低** | ⚠️ 严格只动 indicator。注意:底部主导航是 NavigationBar(选中胶囊已自带动画),不在本项范围。

### 29. 点赞/收藏波纹微交互
- **效果**:集市点赞时心形从空到实弹出+一圈波纹扩散;收藏时星标弹跳。微交互让交互"有奖励感"。
- **落点**:`MarketListScreen` 点赞按钮、收藏按钮。`animateScale` + 波纹 `Canvas`。
- **成本:低**。

### 34. 启动闪屏 → 首页过渡
- **效果**:冷启动闪屏显 App logo,启动完成后 logo 平滑过渡(缩放/淡出)到首页对应位置,而非硬切,启动更连贯有质感。
- **落点**:**先引入** `androidx.core:core-splashscreen` 依赖(项目当前无,前置成本);配 splash theme;保持式闪屏 `setKeepOnScreenCondition` + `onExitAnimationContainer` 做 logo 过渡。
- **成本:中** | 引依赖 + 主题配置 + 过渡动画;⚠️ 见 D7,严控不拖慢冷启动(<500ms)。

### 44. 动态天气图标 ⭐
- **效果**:天气图标本身会动 — 晴天太阳缓旋放光、多云云朵飘动、下雨雨线落下、下雪雪花飘转、夜晚月亮微呼吸。从静态 ImageVector 升级为活图标,天气页沉浸感大增。
- **落点**:`WeatherCode.kt:44`(静态 `icon()`)旁新建 `DynamicWeatherIcon(code)` composable,按 code 用 `Canvas` + `rememberInfiniteTransition` 绘制动画;落 `WeatherScreen` CurrentWeatherCard、`WeatherPanel`。
- **成本:中** | 每种天气一套动画,分批做(首批见 D5)。⚠️ 性能用 `Canvas` 而非多层 Composable,避免卡顿。

### 10. 季节/节日彩蛋主题
- **效果**:特定时段 App 自动带彩蛋 — 开学季首页 banner 加"新学期加油"、考试周倒计时角标、校庆(9 月)logo 戴帽、节日配色微调/小装饰。不改功能,纯氛围。
- **落点**:`SessionManager` 或新 `SeasonalTheme` 判断日期 → 触发 UI 装饰;接 8 的主题色系统可做节日配色。
- **成本:低中** | ⚠️ 维护成本,触发场景见 D6(默认 3 个节点)。

**批次四完成标志**:子页 Tab 指示器滑动、集市有点赞波纹、冷启动有 logo 过渡闪屏、天气有动态图标、节日有彩蛋氛围。

---

## 7. 交付检查清单(每批结束前过一遍)

- [ ] 深色/浅色双主题都验证过(尤其批次一 71、批次三 58)
- [ ] 无新硬编码 `Color(0xFF...)`(走 token/colorScheme)
- [ ] 渐变未滥用(限 Hero/按钮/描边/背景光斑)
- [ ] 玻璃/模糊效果低版本(<31)有降级
- [ ] Tab 内容区未用 Crossfade(仅指示器动)
- [ ] 卡片大字号下不溢出(40 字号可调)
- [ ] 有缓存时错误不全屏覆盖(55)
- [ ] 改动同步更新 `docs/13-UI主题与设计系统.md`(token/组件变化时)
- [ ] 跑 `:app:testDebugUnitTest` + `assembleDebug`(交付前)

---

## 8. 备注

- 三个校准项(避免重复造轮子):
  - **课表(61/62)已是强项** — WeekGrid 自绘网格+8 调色板+三样式课程卡+当前时间红线已完整,本计划不再单独立项,仅在 30 中把时间线从 30s 跳变平滑化。
  - **成绩图表(63)已部分落地** — GpaBarChart 已存在,本计划未单列 63(用户未选);如需趋势折线/学期对比可后续追加。
  - **今日课程卡/应用中心已丰富** — 不推倒,仅做按压反馈+渐变 token 收口。
- 本文件是工程计划,非架构文档;token/组件定型后同步进 `docs/13`。
