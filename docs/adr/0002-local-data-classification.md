# ADR-0002：本地数据分类

- 状态：Accepted

## 背景

`SessionManager` 同时处理凭据、账号状态、用户资产、设置和缓存，清理与迁移边界容易互相影响。

## 决策

本地数据按敏感凭据、账号状态、用户资产、应用设置、业务缓存和作业状态分类。每类数据拥有独立清理语义；普通退出保留用户资产和设置，清除全部数据才删除所有类别。

## 影响

M2 将保留 `SessionManager` 兼容门面并逐类委托新实现，不以目录拆分代替行为边界。
