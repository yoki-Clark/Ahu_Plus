# ADR-0006：匿名指标默认关闭并显式同意

- 状态：Accepted

## 背景

生产故障需要聚合信号，但账号、课程、网络请求和稳定设备标识会引入不可接受的隐私风险。

## 决策

匿名指标默认关闭。只有完成隐私评审、更新政策版本并获得显式同意后，才能按字段 allowlist 收集 Module 级结果计数、API level 和 App 版本。安装标识如确有必要必须可重置并有保留周期。

## 影响

M4 在法律与产品条件满足前不得接入遥测 SDK 或上传任何指标；诊断反馈继续由用户主动预览和提交。
