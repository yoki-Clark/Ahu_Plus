# ADR-0005：控制面只能降低能力

- 状态：Accepted

## 背景

协议失效时需要快速止损，但控制面不应成为启动和校园功能的单点故障，也不能扩大客户端权限。

## 决策

远程配置经签名验证后只能提示、降级或关闭能力，不得开启本地关闭的第三方或高风险功能，不得下发任意端点、代码、JavaScript 或凭据探针。验签失败或控制面不可用时使用本地安全默认或最后合法配置。

## 影响

客户端始终能启动并读取缓存；运营侧无法通过配置绕过用户同意或网络安全边界。
