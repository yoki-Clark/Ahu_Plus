# ADR-0003：用户资产采用跨稳定版本渐进迁移

- 状态：Accepted

## 背景

一次性替换本地存储会放大旧键遗漏、迁移中断和回滚失败的风险。

## 决策

迁移按数据类别分批执行：首个版本双读并保留旧键，第二个稳定版本停止旧写入但继续保留旧键，第三个稳定版本经迁移测试和反馈确认后清理兼容代码。迁移必须幂等并记录终态。

## 影响

迁移周期变长，但任一稳定版本均可独立发布和回滚，失败不会清空其他数据类别。
