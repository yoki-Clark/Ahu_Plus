<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- 2026-06-22 新增:WidgetUpdateScheduler / BootReceiver / 课程提醒 所需 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.WRITE_CALENDAR" />

    <!-- 2026-06-22 新增:学习通后台学习 ForegroundService 所需 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- 学习通后台悬浮窗 (可选) -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- 2026-06-24 新增:超星位置签到(GPS) + 拍照/二维码签到 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />

    <queries>
        <package android:name="com.tencent.mm" />
    </queries>

    <application
        android:name=".AhuPlusApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ahu_Plus">
        <receiver
            android:name=".ui.widget.TodayScheduleWidgetReceiver"
            android:exported="true"
            android:label="@string/today_schedule_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/today_schedule_widget_info" />
        </receiver>

        <!-- 课程提醒接收器 (AlarmManager 触发) -->
        <receiver
            android:name=".notification.CourseReminderReceiver"
            android:exported="false" />

        <!-- 日程提醒接收器 (AlarmManager 触发) -->
        <receiver
            android:name=".notification.AgendaReminderReceiver"
            android:exported="false" />

        <!-- Widget / 课程提醒统一调度接收器 (自递归 AlarmManager) -->
        <receiver
            android:name=".notification.WidgetUpdateScheduler"
            android:exported="false">
            <intent-filter>
                <action android:name="com.ahu_plus.widget.ACTION_UPDATE_WIDGETS" />
            </intent-filter>
        </receiver>

        <!-- 开机广播接收器 (重注册所有 PendingIntent) -->
        <receiver
            android:name=".notification.BootReceiver"
            android:exported="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <!-- MY_PACKAGE_REPLACED: 应用自身升级后也会重排 -->
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/Theme.Ahu_Plus"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data
                    android:host="market"
                    android:path="/import"
                    android:scheme="ahuplus" />
            </intent-filter>
        </activity>

        <!-- 2026-06-22 新增:学习通后台学习 ForegroundService (悬浮窗进度) -->
        <service
            android:name=".service.ChaoxingStudyService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <!-- 2026-06-27 新增:WeLearn 随行课堂后台刷课 ForegroundService -->
        <service
            android:name=".service.WeLearnStudyService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>

</manifest>
